import unittest
import uscore.models.tabulate

class TestTabulator(unittest.TestCase):
    PICKLED_POSTS = ...
    # TODO: we need to rewrite the original scraper first so that we have all of our info in the right format
